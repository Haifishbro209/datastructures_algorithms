from create_array import random_array
from random import randint

def quicksort(arr):
    length = len(arr)
    split_point = arr[randint(0,length)]
    
    for i in range(length):
        




arr = random_array(100)